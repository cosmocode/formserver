{% from '_macros' import renderError, renderTooltip %}
<div class="column {{ column is not empty ? column : 'is-full' }}">
    <div class="field">
        <label class="label" for="{{ id }}">{{ label }} {{ is_required ? '*' }}</label>
        {{ renderTooltip(tooltip) }}
        <div class="control">
                <textarea
                    id="{{ id }}"
                    name="{{ id }}"
                    {{ cols is defined ? 'cols='~cols }}
                    {{ rows is defined ? 'rows='~rows }}
                    class="textarea form-input {{ errors is not empty ? 'is-danger' }}"
                    {{ placeholder is not empty ? ('placeholder="' ~ placeholder ~ '"') | raw }} />

                    {{- value -}}
                </textarea>
        </div>
        {{ renderError(errors) }}
    </div>
</div>