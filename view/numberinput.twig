{% from '_macros' import renderError, renderTooltip %}
<div class="column {{ column is not empty ? column : 'is-full' }}">
    <div class="field">
        <label class="label" for="{{ id }}">
            {%- if label is not empty -%}
                {{ label }} {{ is_required ? '*' }}
            {%- endif  -%}
        </label>
        {{ renderTooltip(tooltip) }}
        <div class="control">
            <input
                    type="number"
                    id="{{ id }}"
                    name="{{ id }}"
                    value="{{ value }}"
                    class="input form-input {{ errors is not empty ? 'is-danger' }}"
                    {% if placeholder is not empty -%}
                        placeholder="{{ placeholder ~ (is_required and label is empty ? ' *') }}"
                    {%- endif %} />
        </div>
        {{ renderError(errors) }}
    </div>
</div>